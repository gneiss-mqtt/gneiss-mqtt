[package]
name = "gneiss-mqtt-client-async"
license = "Apache-2.0"
version = "0.1.0"
edition = "2021"
keywords = ["mqtt", "mqtt5", "iot"]
categories = [ "asynchronous", "network-programming" ]
description = "Asynchronous MQTT client"
homepage = "https://github.com/gneiss-mqtt/gneiss-mqtt-client-async"
documentation = "https://docs.rs/gneiss-mqtt-client-async"
repository = "https://github.com/gneiss-mqtt/gneiss-mqtt-client-async"
readme = "README.md"
resolver = "2"

[features]
strict = [] # treats warnings as errors, only intended for internal checks
testing = [ "dep:assert_matches", "dep:test-case", "gneiss-mqtt/testing" ]

[package.metadata.docs.rs]
features = []
rustdoc-args = ["--cfg", "docsrs"]

[dependencies]
gneiss-mqtt = { path = "../gneiss-mqtt" }
assert_matches = { version = "1", optional = true } # only included in test builds
test-case = { version = "3", optional = true }

[dev-dependencies]
gneiss-mqtt-client-async = { path = ".", features = [ "testing" ]}